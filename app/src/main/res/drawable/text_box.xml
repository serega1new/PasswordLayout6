<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners
        android:radius="@dimen/text_box_corners_radius"/>

    <stroke
        android:width="@dimen/text_box_stroke_width"
        android:color="@android:color/darker_gray"
        android:dashWidth="@dimen/text_box_stroke_dashWidth"
        android:dashGap="@dimen/text_box_stroke_dashGap" />

</shape>