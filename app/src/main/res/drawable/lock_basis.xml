<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">



    <size
        android:width="@dimen/lock_basis_size_width"
        android:height="@dimen/lock_basis_size_height" />

    <corners
        android:radius="@dimen/lock_basis_corners_radius"/>

    <stroke
        android:width="@dimen/lock_basis_stroke_width"
        android:color="@android:color/darker_gray"
        android:dashWidth="@dimen/lock_basis_stroke_dashWidth"
        android:dashGap="@dimen/lock_basis_stroke_dashGap" />


</shape>